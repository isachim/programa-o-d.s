from abc import ABC # abstract base clases

from collections.abc import MutableSequemce
from numbers import Complex
Class Numero (Complex):
    def __getitem__(self, item) :
        super().__getitem__()

filmes = Numero()
